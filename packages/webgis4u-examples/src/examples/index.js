/**
 * @typedef ExampleModuleLoader
 * @property {string} name The name of the module
 * @property {Function} load The function to load the module
 * @property {string} source The module source code
 */

/**
 * @typedef ExampleModule
 * @property {Function} initialize Function to initialize the module
 */

export * from './basemap';
export * from './controls';
