import Orthofoto from './Orthofoto';

describe('ugis.source.basemap.Orthofoto', () => {
  it('should be created', () => {
    const instance = new Orthofoto();
    expect(instance).not.toBeNull();
  });
});
